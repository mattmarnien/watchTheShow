<div class="container">
    <div class="row holderDiv">
      
        <div id="archivedDiv" class='col-12 text-left'>
        
              {{#each shows}}
                    {{#if archived}}
                     <div class="showDiv row">
                        <div class="nameDiv col-10"><h4>{{name}}</h4></div>
                        <div class="buttonDiv col-2"> <button class="backToWatchList btn" data-id="{{id}}"> Put Back on Watchlist </button></div>
                    </div>
                    {{/if}}
                {{/each}}
    
        </div>
    </div>

<hr>

<div class="buttonDiv col-4"> <a href="/"><button class="toIndex btn"> Go Home <i class="fas fa-home"></i></button></a></div>

</div>